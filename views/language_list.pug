extends layout

block content
  h3= title
  hr
  .container
    #unorderedList.ul
      each language in language_list
        li
          a(href=language.url) #{language.name}
      else
        li There are no languages.