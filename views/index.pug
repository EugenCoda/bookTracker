extends layout

block content
  h3= title
  p Track your read books, as well as of ones on the wish list.

  if error
    p Error getting dynamic content.
  else
    p Our library has the following record counts:

    ul
      li #[strong Books:] !{data.book_count}
      //- minus the default author
      li #[strong Authors:] !{data.author_count-1}
      li #[strong Genres:] !{data.genre_count}
      li #[strong Languages:] !{data.language_count}
      li #[strong Countries:] !{data.country_count}
  if !user
    a.btn.btn-primary(href='users/register') Start Now 
    | 
    a.btn.btn-dark(href='users/login') Login 
